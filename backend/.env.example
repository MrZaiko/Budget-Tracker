# Required
DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/budget_tracker

# OIDC (required in production)
OIDC_ISSUER_URL=https://auth.example.com/application/o/alpha/
OIDC_AUDIENCE=budget-tracker

# Currency settings
BASE_CURRENCY=EUR
FRANKFURTER_BASE_URL=https://api.frankfurter.app

# Scheduler
SCHEDULER_TIMEZONE=UTC

# Local auth (dev/debug only — NEVER enable in production)
LOCAL_AUTH_ENABLED=false
LOCAL_AUTH_SECRET=change-me-to-a-random-secret-at-least-32-chars

# App
DEBUG=false
